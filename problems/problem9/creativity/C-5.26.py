# Sorting the array from 1 to n
# Check each element if the next element is the same

# w/o sorting:
# Create a set "spotted". Add all elements that are not contained yet.
# If it is already contained then this is the first number.
